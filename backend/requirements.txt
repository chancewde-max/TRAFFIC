# Core API
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<3.0
pydantic>=2.7,<3.0
websockets>=12.0

# DDOT live camera registry (optional at runtime, but always installed so
# CAMERA_MODE=live works without an extra install step)
paho-mqtt>=2.1,<3.0

# --- Optional: only needed for CAMERA_MODE=live (real video + detection) ---
# Uncomment to enable real computer-vision tracking from camera video.
# These are heavy (torch pulls in ~1-2GB) and unnecessary for mock mode.
# opencv-python-headless>=4.9
# ultralytics>=8.2
# torch>=2.2
