# Core API
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<3.0
pydantic>=2.7,<3.0
websockets>=12.0

# DDOT live camera registry (optional at runtime, but always installed so
# CAMERA_MODE=live works without an extra install step)
paho-mqtt>=2.1,<3.0
# Only used if HTTPS_PROXY is set in the environment (routes the MQTT socket
# through an HTTP CONNECT proxy) -- harmless/unused otherwise.
pysocks>=1.7,<2.0

# --- CAMERA_MODE=live: real video + detection ---
# torch is installed separately (see Dockerfile / README) from PyTorch's
# CPU-only wheel index -- `--extra-index-url` here isn't enough to force it,
# since pip still considers the default PyPI index's much larger
# CUDA-bundled build a valid (often preferred) match.
opencv-python-headless>=4.9
ultralytics>=8.2
